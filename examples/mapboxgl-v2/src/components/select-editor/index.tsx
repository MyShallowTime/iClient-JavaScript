import React from 'react';
import { Select, SelectProps } from '@ispeco/iptl-components-react';
import './style';

const SelectEditor = (props: SelectProps) => {
    return <Select {...props}></Select>
}

export default SelectEditor;
